@model List<OnlineShopWebApp.Models.Product>
@{
    ViewData["Title"] = "Товары";
}

<div class="row mt-3">
    @await Html.PartialAsync("AdminMenu")
    <div class="col-9 mb-3">
        <div class="text-center text-uppercase text-success mb-5 mt-5">
            <h2 class="row-title">Товары</h2>
        </div>

        @if (Model != null && Model.Count > 0)
        {
            <div class="row justify-content-center mt-3">
                <div class="col-12">
                    <div class="text-end">
                        <a class="btn btn-outline-success mb-2 mt-2"
                           asp-controller="Products" asp-action="Create">
                            Добавить новый продукт
                        </a>
                    </div>
                    <table class="table table-responsive table-striped table-hover mb-5">
                        <thead>
                            <tr align="center">
                                <th scope="col">#</th>
                                <th scope="col">Наименование товара</th>
                                <th scope="col">Цена, руб.</th>
                                <th scope="col"></th>
                                <th scope="col"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @for (var i = 0; i < Model.Count; i++)
                            {
                                <tr align="center" class="align-baseline">
                                    <td class="fw-bold">@(i + 1)</td>
                                    <td align="left">
                                        <a class="text-success"
                                           asp-controller="Products"
                                           asp-action="Detail"
                                           asp-route-id="@Model[i].Id">
                                            <h5>@Model[i].Name</h5>
                                        </a>
                                    </td>
                                    <td>@Model[i].Cost.ToString("C")</td>
                                    <td>
                                        <a class="btn btn-outline-success"
                                           asp-controller="Products"
                                           asp-action="Edit"
                                           asp-route-id="@Model[i].Id">
                                            Редактировать
                                        </a>
                                    </td>
                                    <td>
                                        <button type="button" class="btn btn-outline-danger" data-bs-toggle="modal" data-bs-target="#deleteProductModal_@Model[i].Id">
                                            Удалить
                                        </button>
                                        <div class="modal fade" id="deleteProductModal_@Model[i].Id" tabindex="-1" aria-labelledby="deleteProductModalLabel_@Model[i].Id" aria-hidden="true">
                                            <div class="modal-dialog">
                                                <div class="modal-content">
                                                    <div class="modal-header">
                                                        <h5 class="modal-title" id="deleteProductModalLabel_@Model[i].Id">Удаление товара</h5>
                                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                                    </div>
                                                    <div class="modal-body">
                                                        Вы действительно хотите удалить товар "<strong>@Model[i].Name</strong>"?
                                                    </div>
                                                    <div class="modal-footer">
                                                        <form method="post" asp-area="Admin" asp-controller="Products" asp-action="Delete" asp-route-id="@Model[i].Id">
                                                            <button type="submit" class="btn btn-outline-danger">Удалить</button>
                                                        </form>
                                                        <button type="button" class="btn btn-outline-success" data-bs-dismiss="modal">Отмена</button>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                        <tfoot>
                            <tr align="center">
                                <th colspan="2"></th>
                                <th colspan="3">
                                    Общее количество товаров: @Model.Count
                                </th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
        }
        else
        {
            <div class="text-center">
                <h2 class="row-title">В хранилище нет товаров!</h2>
            </div>
            <div class="text-center mt-5">
                <a class="btn btn-outline-success mb-2 mt-2"
                   asp-controller="Products" asp-action="Create">
                    Добавить новый продукт
                </a>
            </div>
        }
    </div>
</div>