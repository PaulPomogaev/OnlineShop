@using OnlineShop.Db.Models
@using OnlineShop.Core.Models

@model Role
@{
    ViewData["Title"] = "Добавление роли";
}

<div class="row mt-3">
    @await Html.PartialAsync("AdminMenu")
    <div class="col-9 mb-3">
        <div class="text-center text-uppercase text-success mb-5 mt-5">
            <h2 class="row-title">Добавление новой роли</h2>
        </div>

        <form method="post" asp-area="Admin" asp-controller="Roles" asp-action="Add">
            <div asp-validation-summary="All" class="text-danger fw-bold text-uppercase mb-3"></div>
            <div class="row justify-content-center mt-3">
                <div class="col-6 mb-3">
                    <div class="mb-3">
                        <label asp-for="Name" class="form-label"></label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </div>
            </div>
            <div class="text-center m-3">
                <button class="btn btn-outline-success" type="submit">
                    Добавить
                </button>
            </div>
        </form>
    </div>
</div>
